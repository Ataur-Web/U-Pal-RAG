PORT=3000
ADMIN_PASSWORD=

# Ollama LLM integration (optional — bot works without it)
# Set OLLAMA_URL to your Cloudflare tunnel URL when running Ollama locally
OLLAMA_URL=https://your-tunnel.trycloudflare.com
OLLAMA_MODEL=llama3.1:8b

# MongoDB (optional — feedback saved to local file without it)
MONGODB_URI=
